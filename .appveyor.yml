# Sharpmake AppVeyor CI configuration

image: Visual Studio 2015

platform:
 - Any CPU

configuration:
 - Debug
 - Release

before_build:
 - cmd: dotnet restore

build:
  project: Sharpmake.sln
  verbosity: minimal

after_build:
  - cmd: 7z a Sharpmake.zip %APPVEYOR_BUILD_FOLDER%\Sharpmake.Application\bin\$(configuration)\*

after_test:
 - cmd: python regression_test.py

artifacts:
  - path: Sharpmake.zip
    name: Sharpmake
    type: zip
